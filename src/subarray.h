#ifndef _SUBARRAY_H_
#define _SUBARRAY_H_
#include <limits.h>
#include <stdlib.h>
#include <string.h>

void find_max_cross_subarray(int*, int, int, int, int*);
int* find_maximun_subarray(int*, int, int);

#endif